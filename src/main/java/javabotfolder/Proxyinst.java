package javabotfolder;

import java.lang.reflect.Proxy;
import java.util.Map;

public class Proxyinst {
  //  Map proxyInstance = (Map) Proxy.newProxyInstance(invochandler.class.getClassLoader(), new Class[] { Map.class }, new invochandler());


}
